extends layout

block content
  .container
    .row.justify-content-center.mt-5
      .col-md-6
            form(action="/login", method="POST")
              .mb-3
              if error
                p.text-danger.text-center= error
              .text-center.mt-3
                button(type="submit" class="btn btn-primary btn-lg") Login to Pixiv
